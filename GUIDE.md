# 특별실 예약 관리 웹앱 — 안내서

이 문서는 시스템의 구조와 기능, 고치는 방법, 담당자가 바뀔 때 해야 할 일을 정리한 것입니다.

- 최종 갱신: 2026-08-28
- 함께 볼 문서: 개발 메모 [`CONTEXT.md`](CONTEXT.md) · 변경 내역 [`CHANGELOG.md`](CHANGELOG.md) · 요약 [`README.md`](README.md)

---

## 1. 이 시스템은 무엇인가

교직원이 브라우저로 학교 특별실(강당, 운동장, 시청각실, 도서관, 컴퓨터실 등)을 예약하는 웹앱입니다.

- 서버 비용이 들지 않습니다. Google과 GitHub의 무료 서비스만 사용합니다.
- 로그인이 없습니다. 예약할 때 이름을 적으며, 접속 주소는 교내에서만 공유합니다.

### 현재 운영 상태 (2026-08-28)

| 항목 | 내용 |
|---|---|
| 상태 | 학교에서 실제 운영 중 |
| 접속 주소 | GitHub Pages, Vercel 두 곳(같은 코드). 저장소에 반영하면 자동으로 다시 배포됩니다. |
| 소스 저장소 | `shinkim0712/specialroom` |
| 데이터 | 특별실 9개, 기간 정규시간 100건 이상, 예약 상시 |
| 서버 실행 계정 | 처음 배포한 사람의 개인 Google 계정 (→ 6장) |

---

## 2. 어떻게 만들어져 있나

### 구성

```
교직원 브라우저  ──(화면 요청)──▶  GitHub Pages / Vercel  (HTML·CSS·JS 파일만 보관)
      │
      └──(예약 저장·조회)──▶  Google Apps Script  ──(읽고 쓰기)──▶  Google Sheets
                              (Code.gs, 서버 역할)                   (데이터 저장)
```

| 부분 | 하는 일 |
|---|---|
| 화면 (HTML·CSS·JS) | 시간표를 그리고 사용자 입력을 처리합니다. 별도 빌드 과정 없이 파일 그대로 사용합니다. |
| GitHub Pages / Vercel | 위 파일들을 브라우저에 전달만 합니다. 로직은 없습니다. |
| Google Apps Script (`Code.gs`) | 예약 저장·조회 요청을 받아 시트를 고칩니다. 관리자 비밀번호도 여기서 확인합니다. |
| Google Sheets | 데이터를 보관합니다. 스프레드시트 한 개이고, 탭 하나가 표 하나입니다. |

### 오프라인에서도 동작

브라우저는 데이터를 자기 안에도(`localStorage`) 저장합니다. 서버가 잠깐 죽어도 마지막으로 본 화면은 그대로 보이고, 예약도 로컬에 저장됐다가 서버가 돌아오면 자동으로 맞춰집니다.

---

## 3. 파일

```
specialroom/
├── index.html      화면 뼈대와 팝업. config → api → app 순서로 스크립트를 불러옵니다.
├── style.css       디자인
├── config.js       구글시트 연결 주소 설정. 이 파일의 apiUrl만 바꾸면 다른 시트로 연결됩니다.
├── api.js          Apps Script와 통신하는 함수 모음
├── app.js          클라이언트 전체 로직 (약 1,400줄)
└── apps-script/
    └── Code.gs      서버 코드. Apps Script 편집기에 붙여넣어 씁니다.
                     (.gitignore로 저장소에서 제외 — 시트 ID와 관리자 비밀번호가 들어 있어서)
```

### 캐시 갱신 (꼭 기억할 것)

`index.html` 안에서 `app.js?v=57`, `style.css?v=39` 처럼 파일명 뒤에 버전 번호를 붙여 부릅니다.

> **CSS나 JS 파일을 고치면, `index.html`에 있는 그 파일의 `?v=` 번호를 1 올려야 합니다.**
> 안 올리면 브라우저가 예전 파일을 계속 쓰기 때문에 "고쳤는데 왜 그대로지?" 상태가 됩니다.

---

## 4. 데이터 (Google Sheets 탭)

없는 탭은 서버가 처음 실행될 때 알아서 만듭니다. 직접 만들 필요 없습니다.

### `reservations` — 예약
`id · room · date · period · name · classroom · purpose · passwordHash · createdAt`

- `date`: `2026-09-01` 형식
- `period`: `1`~`8`, `4A`(4교시 저학년), `4E`(4교시 고학년), `5EH`(5교시). 정확한 값은 `app.js`의 `PERIODS`
- `classroom`: `3학년 5반` 형식, 또는 회의·행사면 `회의/행사`
- `passwordHash`: 안 쓰는 칸(항상 비어 있음)

### `rooms` — 특별실
`name · order` (order는 탭 순서)

### `dateRules` — 기간 정규시간 / 예약 금지
`id · room · startDate · endDate · periods · daysOfWeek · label · blocked`

- 정규시간 정보는 전부 이 탭에 있습니다.
- `periods`, `daysOfWeek`는 `["1","2"]`, `["월","수"]` 같은 형태로 저장됩니다.
- `blocked`가 `true`면 그 칸은 예약이 막힙니다.

### `holidays` — 학교자체 휴일
`id · startDate · endDate · label` (방학, 재량휴업일 등)

- 법정공휴일은 이 탭이 아니라 `app.js`의 `KR_HOLIDAYS` 목록에 있습니다(현재 2026~2028년치).

### `logs` — 변경 기록
`ts · action · summary`

- 예약·규칙·특별실·휴일이 지워지거나 수정되거나 라벨이 가려지면 한 줄씩 남습니다.
- **누가 했는지는 안 남깁니다. 무엇이 없어졌는지만 남깁니다.**
- 3,000줄까지 보관하고 넘으면 오래된 것부터 지웁니다.

### `schedule` — 안 쓰는 탭
`room · dayOfWeek · period · label`

- 예전 "요일별 정규시간" 기능이 2026-08-28에 없어졌습니다. 탭은 남아 있지만 앱이 안 씁니다.

---

## 5. 어떻게 동작하나

### 서버와 통신

- 조회: `주소/exec?action=loadAll` → 전체 데이터를 받습니다.
- 저장: `주소/exec`에 `{"action":"create", ...}` 형태로 보냅니다.

### 모든 변경은 즉시 저장됨

예약, 특별실, 기간 정규시간, 학교휴일 — 무엇을 바꾸든 **그 즉시 서버에 저장**됩니다. 따로 "저장" 버튼을 누를 필요가 없습니다.

| 데이터 | 서버에서 처리하는 이름 |
|---|---|
| 예약 | `create` / `update` / `delete` |
| 특별실 | `addRoom` / `deleteRoom` / `reorderRooms` |
| 기간 정규시간 | `createDateRule` / `updateDateRule` / `deleteDateRule` |
| 학교휴일 | `createHoliday` / `updateHoliday` / `deleteHoliday` |

- 서버는 요청 하나씩 잠금을 걸고 처리합니다. 두 사람이 같은 시간대를 동시에 예약하면 한 명만 됩니다.
- 저장이 실패하면 화면을 원래대로 되돌리고 알려줍니다.

> 예전에는 정규시간·특별실을 버튼으로 한꺼번에 저장했는데, 데이터가 덜 불러와진 상태로 저장하면 서버 데이터가 날아가는 사고(2026-08-27)가 있어서 전부 즉시 저장 방식으로 바꿨습니다.

### 자동 새로고침

- **사용자 모드**: 60초마다 서버에서 다시 받아와 화면을 갱신합니다(남의 예약 반영).
- **관리자 모드**: 편집 중 화면이 바뀌지 않도록 자동 조회를 멈춥니다. 단, 멈추는 건 받아오는 쪽뿐이고, 관리자가 바꾼 건 즉시 서버로 나갑니다.
- **처음 접속할 때**: 모드와 관계없이 한 번 전체를 받아옵니다.

### 관리자 비밀번호

- 브라우저 코드에는 없습니다. `Code.gs`의 `ADMIN_PW`에만 있고, 서버가 확인합니다.
- 서버 연동이 꺼져 있으면 관리자 모드에 못 들어갑니다.

### config.js 자동 반영

`config.js`의 `version` 숫자가 바뀌면, 이미 사이트를 써본 브라우저에도 새 `apiUrl` 등이 자동으로 적용됩니다. 담당자가 바뀔 때 이 방식으로 새 시트에 연결합니다.

---

## 6. 기능

### 일반 사용자

| 기능 | 설명 |
|---|---|
| 주간 시간표 | 월~금 × 1~8교시(4교시는 저·고학년 2칸). 달력으로 아무 날짜나 이동 |
| 월별 보기 | 달력에 그날 예약을 "교시 학급"으로 나열 |
| 예약 | 빈 칸 클릭 → 이름·학급(학년/반, 12반까지)·목적. 회의·행사면 학년/반 생략. 이름·학급은 다음 예약에 자동 입력 |
| 예약 수정·삭제 | 비밀번호 없음 |
| 다중 선택 | 빈 칸 여러 개 한꺼번에 예약, 예약 여러 개 한꺼번에 삭제(삭제 시 목록을 보여줌) |
| 내 예약 보기 | 이름으로 검색 |
| 기간 정규시간 편성 | 특별실·기간·요일·교시별 라벨 설정 또는 예약 금지 |
| 특별실 추가 | 사용자도 가능(삭제·순서 변경은 관리자만) |

공휴일·학교휴일은 회색으로 표시되고, 그날도 예약은 됩니다.

### 관리자 (위 기능에 더해)

| 기능 | 설명 |
|---|---|
| 관리자 모드 | 우측 상단 버튼 → 비밀번호 |
| 특별실 삭제 | 탭 우클릭. 그 특별실의 예약·규칙도 함께 삭제되고, 확인창에 건수를 보여줌 |
| 특별실 순서 변경 | 탭을 0.35초쯤 누른 뒤 좌우로 이동 |
| 학교휴일 관리 | 방학·재량휴업일 등 등록·수정·삭제 |
| 변경 기록 | 지워지거나 수정되거나 가려진 내용을 최근 순으로 확인 |
| 구글시트 설정 | 서버 주소, 자동 저장·조회 설정, 서버 데이터 정리, 브라우저 데이터 초기화 |
| 서버에서 불러오기 | 서버 데이터로 화면을 강제로 다시 채움 |

### 예약 금지의 동작

- 막으려는 기간·교시에 이미 예약이 있으면, 그 예약을 지우지 않고 목록만 보여준 뒤 멈춥니다. 예약자에게 연락해 지운 다음 다시 시도해야 합니다.
- 규칙이 여러 개 겹치면, 그중 하나라도 예약 금지면 예약 금지가 먼저 표시됩니다.
- 규칙끼리 덮어써도 지워지지 않아서, 위엣것을 삭제하면 아래 것이 다시 나옵니다.

---

## 7. 새로 만드는 방법 (다른 학교용)

### 7.1 코드 받기

```bash
git clone https://github.com/shinkim0712/specialroom.git
cd specialroom
```

### 7.2 시트와 서버 만들기 (Google)

1. Google Drive에서 새 스프레드시트를 만듭니다(이름 자유).
2. 주소에서 시트 ID를 복사합니다: `https://docs.google.com/spreadsheets/d/【이 부분】/edit`
3. 스프레드시트 메뉴 → 확장 프로그램 → Apps Script
4. 편집기 내용을 다 지우고 `apps-script/Code.gs` 내용을 붙여넣습니다.
5. 맨 위 두 줄을 고칩니다.
   ```js
   const SHEET_ID = '2번에서 복사한 시트 ID';
   const ADMIN_PW = '정할 관리자 비밀번호';
   ```
6. 배포 → 새 배포 → 유형: 웹앱
   - 실행 계정: 나
   - 액세스 권한: 모든 사용자
   - 배포하고 권한을 승인한 뒤, `https://script.google.com/macros/s/.../exec` 주소를 복사합니다.
7. 그 주소 뒤에 `?action=ping`을 붙여 브라우저로 열어 `{"ok":true, ...}`가 나오는지 확인합니다.

### 7.3 코드에 서버 연결하기

`config.js`를 고칩니다.
```js
const APP_CONFIG = {
  version: 3,          // 기존 숫자에서 +1
  apiUrl: 'https://script.google.com/macros/s/.../exec',   // 7.2에서 받은 주소
  serverEnabled: true,
  autoSave: true,
  autoLoad: true,
  autoLoadInterval: 60,
};
```
`index.html`의 `<script src="config.js?v=2">`도 `config.js?v=3`으로 맞춥니다.

### 7.4 사이트 올리기 (GitHub Pages)

```bash
git remote set-url origin https://github.com/【학교 계정】/specialroom.git
git add -A && git commit -m "학교별 설정" && git push -u origin main
```
GitHub 저장소 → Settings → Pages → Source: `main` / `/(root)` → Save
1~2분 뒤 `https://【계정】.github.io/specialroom/`로 접속됩니다. 이 주소를 교직원에게 알려줍니다.

### 7.5 처음 입력할 것

관리자 모드에서 특별실 목록, 정규시간표, 학교휴일을 등록합니다.

### 7.6 공휴일 갱신

`app.js`의 `KR_HOLIDAYS` 목록은 2028년까지만 있습니다. **2029년이 되기 전에** 새 연도 공휴일을 목록에 추가하고 `app.js`의 `?v=` 번호를 올린 뒤 반영합니다.

---

## 8. 고치는 방법

### 화면(HTML·CSS·JS) 수정

```bash
cd specialroom
# 파일 수정
# 고친 파일에 맞춰 index.html의 ?v= 번호를 +1
git add -A && git commit -m "수정 내용" && git push
# 1~2분 뒤 자동 반영
```

로컬에서 미리 보기:
```bash
python3 -m http.server 8123 --directory specialroom
# http://localhost:8123
```
서버 기능을 테스트할 때는 반드시 테스트 전용 시트를 씁니다(9장).

### 서버(Code.gs) 수정

`git push`와 별개로 Apps Script에 직접 넣어야 합니다.
1. `apps-script/Code.gs`를 고칩니다.
2. Apps Script 편집기에서 전체 선택 → 삭제 → 붙여넣기
3. 맨 위 `SHEET_ID` / `ADMIN_PW` 두 줄을 그 환경 값으로 다시 고칩니다.
4. 저장(Ctrl/Cmd + S)
5. 배포 → 배포 관리 → 편집 → 버전을 **"새 버전"**으로 선택 → 배포
   - "새 버전"을 안 고르면 코드가 안 바뀝니다(가장 흔한 실수).
   - "새 배포"를 하면 주소가 바뀌므로 하지 않습니다.

---

## 9. 테스트 규칙

이 앱은 학교에서 실제 운영 중이라, 잘못 건드리면 진짜 예약이 사라집니다. 실제로 2026-08-27에 검증 중 예약 1건, 학교휴일 2건이 삭제된 적이 있습니다.

그래서 **운영과 완전히 분리된 테스트 전용 구글시트와 Apps Script**를 따로 만들어 뒀습니다.

- 브라우저에서 기능을 시험할 때는 개발자 콘솔에서 먼저 실행합니다.
  ```js
  localStorage.setItem('apiUrl', '테스트 Apps Script 주소');
  location.reload();
  ```
- 끝나면 `localStorage.removeItem('apiUrl'); location.reload();`로 운영으로 돌아옵니다.
- 운영 서버에는 조회(`ping`, `loadAll`) 요청만 합니다. 저장·삭제 테스트는 하지 않습니다.
- 테스트 주소는 `CONTEXT.md` 맨 위에 있습니다.

---

## 10. 인수인계 (담당자가 바뀔 때)

### 왜 새로 연결해야 하나

서버는 **처음 배포한 사람의 Google 계정 권한으로 실행**됩니다. 그 계정이 전근·퇴직으로 정지되면 서버가 멈춥니다. 그래서 담당자가 바뀌면 **새 담당자 계정으로 서버를 다시 배포**해야 합니다.

### 기본 절차 (기존 예약을 안 넘기는 경우)

1. 새 담당자가 자기 Google Drive에 새 스프레드시트를 만듭니다.
2. 확장 프로그램 → Apps Script에서 `apps-script/Code.gs`를 붙여넣습니다.
3. 맨 위 두 줄을 고칩니다: `SHEET_ID`(새 시트), `ADMIN_PW`(새 비밀번호)
4. 배포 → 새 배포 → 웹앱(실행: 나 / 액세스: 모든 사용자) → `.../exec` 주소 복사
5. `config.js`를 고칩니다: `apiUrl`을 새 주소로, `version`을 +1
6. `index.html`의 `config.js?v=N`도 +1
7. `git add -A && git commit -m "담당자 인수인계" && git push`
8. 1~2분 뒤 모든 사용자 브라우저가 자동으로 새 시트에 연결됩니다(사용자는 아무것도 안 해도 됩니다).

### 새로 입력할 것 (데이터를 안 넘긴 경우)

특별실 목록, 정규시간표, 학교휴일을 관리자 모드에서 다시 등록합니다.

### 기존 데이터를 넘기는 경우 (선택)

전임 담당자의 협조가 되면, 기존 스프레드시트를 **파일 → 사본 만들기**로 새 담당자 Drive에 복사한 뒤, 그 사본 ID를 새 `Code.gs`의 `SHEET_ID`에 넣으면 예약·정규시간이 그대로 이어집니다.

### 테스트 시트도 함께

`CONTEXT.md`에 적힌 테스트 전용 시트와 Apps Script도 새 담당자 계정으로 다시 만들어 두는 것이 안전합니다.

---

## 11. 알아둘 제약

| 항목 | 내용 |
|---|---|
| 특별실 삭제 시 건수 | 관리자 모드는 자동 새로고침이 꺼져 있어, 삭제 확인창의 "예약 N건 함께 삭제" 숫자가 최신이 아닐 수 있습니다. 삭제 전에 사용자 모드로 나갔다 오거나 "서버에서 불러오기"로 갱신하는 것이 안전합니다. |
| 학교휴일 기간 겹침 | 겹침을 검사하지 않습니다. |
| 예약 금지 차단 | 브라우저 화면에서만 막습니다. 서버 API를 직접 호출하는 경우는 막지 않습니다(일반 사용 시 문제 없음). |
| 브라우저 저장 용량 | 예약이 수천 건 이상 쌓이면 브라우저 저장 용량 한계에 닿을 수 있습니다. 오래된 예약은 주기적으로 정리하는 것이 좋습니다. |
| HTTPS 필요 | 예약 ID를 만드는 기능이 HTTPS에서만 동작합니다. GitHub Pages·Vercel은 HTTPS라 정상이며, HTTP로 접속하면 예약이 안 만들어집니다. |
| 저장소에 주소 노출 | `config.js`와 문서에 시트 ID와 Apps Script 주소가 들어 있습니다. Apps Script 주소는 원래 모두에게 공개되는 것이라 문제없지만, 저장소를 공개로 둘 거면 다른 민감 정보를 넣지 않아야 합니다. 관리자 비밀번호는 저장소에서 제외된 `Code.gs`에만 있습니다. 이 상태를 유지해야 합니다. |
