# 특별실 예약 관리 웹앱 — 운영·인수인계 안내서

| 항목 | 내용 |
|---|---|
| 문서 목적 | 본 시스템의 구조·기능·구축 방법·유지보수 절차·인수인계 방법을 정리하여, 담당자가 바뀌더라도 운영이 중단되지 않도록 함 |
| 대상 독자 | 시스템 담당 교사, 후임 담당자, 동일 시스템을 도입하려는 타 학교 담당자 |
| 최종 갱신 | 2026-08-28 |
| 관련 문서 | 개발 상세 메모 [`CONTEXT.md`](CONTEXT.md) · 버전별 변경 내역 [`CHANGELOG.md`](CHANGELOG.md) · 요약 [`README.md`](README.md) |

---

## 1. 개요

### 1.1 시스템 정의

교직원이 웹 브라우저를 통해 학교 특별실(강당, 운동장, 시청각실, 도서관, 컴퓨터실 등)의 사용을 예약·관리하는 웹 애플리케이션입니다.

### 1.2 설계 원칙

- **운영 비용 없음.** Google 및 GitHub이 무상으로 제공하는 서비스만으로 구성합니다(Apps Script, Sheets, GitHub Pages).
- **로그인 없음.** 예약 시 이름을 입력하며, 접속 주소는 교내에서만 공유합니다.
- **가독성 우선.** 주 사용자가 정보기술에 익숙하지 않은 교사임을 고려하여, 색상·기호보다 문장으로 이해되는 화면을 지향합니다.

### 1.3 현재 운영 현황 (2026-08-28 기준)

| 항목 | 내용 |
|---|---|
| 운영 상태 | 학교 현장에서 상시 운영 중 |
| 배포 경로 | GitHub Pages 및 Vercel(동일 코드를 이중 배포하며, 저장소에 반영 시 자동 재배포됨) |
| 소스 저장소 | `shinkim0712/specialroom` |
| 데이터 규모 | 특별실 9개, 기간 정규시간 100건 이상, 예약 상시 발생 |
| 서버 실행 주체 | 최초 배포자의 개인 Google 계정 (제6장 인수인계 참조) |

---

## 2. 시스템 구성

### 2.1 구성도

```
┌─────────────────┐   ① 화면 요청       ┌──────────────────────┐
│  교직원 브라우저   │ ─────────────────▶ │  GitHub Pages / Vercel │
│  (HTML/CSS/JS)   │ ◀───────────────── │  (정적 파일 호스팅)     │
└────────┬────────┘   HTML/CSS/JS 응답   └──────────────────────┘
         │
         │ ② 데이터 저장·조회 (HTTP fetch, JSON)
         ▼
┌─────────────────────┐   ③ 시트 조작       ┌──────────────────┐
│  Google Apps Script  │ ─────────────────▶ │  Google Sheets    │
│  (Code.gs, 서버 역할)  │ ◀───────────────── │  (데이터베이스 역할)  │
└─────────────────────┘                    └──────────────────┘
```

### 2.2 구성 요소별 역할

| 구성 요소 | 역할 | 비고 |
|---|---|---|
| 프론트엔드 | 화면 표시 및 사용자 조작 처리 | 순수 HTML/CSS/JavaScript. 프레임워크·빌드 과정 없음 |
| 호스팅 (GitHub Pages / Vercel) | 정적 파일 전송 | 별도 로직을 수행하지 않음 |
| 서버 (Google Apps Script) | 데이터 저장·조회 요청 처리, 관리자 인증 | 웹앱 1개(`Code.gs`). `/exec` 주소로 요청 수신 |
| 데이터베이스 (Google Sheets) | 데이터 영구 저장 | 스프레드시트 1개. 각 탭이 하나의 테이블에 대응 |

### 2.3 오프라인 동작

브라우저는 조회한 데이터를 `localStorage`에도 보관합니다. 서버 장애 시에도 최근에 조회한 상태는 화면에 유지되며, 예약도 로컬에는 저장되어 서버 복구 후 자동 동기화됩니다. 서버 연동을 비활성화한 경우에도 단독 실행형 앱처럼 동작합니다.

---

## 3. 파일 구성

```
specialroom/
├── index.html      화면 구조 및 모달(팝업) 정의. config → api → app 순으로 스크립트를 로드함
├── style.css        디자인 (Slate Blue #0e3b69 계열)
├── config.js        구글시트 연결 설정. apiUrl 값 변경만으로 연결 대상 시트를 교체함
├── api.js           Apps Script 통신 함수 모음 (HTTP fetch 래퍼)
├── app.js           클라이언트 전체 로직 (약 1,400행)
├── apps-script/
│   └── Code.gs       서버 코드. Apps Script 편집기에 붙여넣어 사용
│                     (.gitignore로 저장소에서 제외됨 — 시트 ID 및 관리자 비밀번호 포함)
├── .claude/launch.json   로컬 미리보기 설정 (python http.server 8123)
├── README.md · CONTEXT.md · CHANGELOG.md · GUIDE.md(본 문서)
```

### 3.1 캐시 갱신 규칙 (필수)

`index.html`은 `style.css?v=39`, `app.js?v=57`, `api.js?v=22`, `config.js?v=2`와 같이 파일명에 버전 번호를 부여하여 로드합니다.

> **CSS 또는 JavaScript 파일을 수정할 때마다 해당 파일의 `?v=` 번호를 1씩 증가시켜야 합니다.** 번호를 올리지 않으면 브라우저가 캐시된 이전 파일을 계속 사용하여 수정 사항이 반영되지 않습니다.

---

## 4. 데이터 모델

Google Sheets의 각 탭이 하나의 테이블입니다. 없는 탭은 서버(`Code.gs`)가 최초 호출 시 자동으로 생성하므로 수동으로 만들 필요가 없습니다.

### 4.1 `reservations` — 예약

| id | room | date | period | name | classroom | purpose | passwordHash | createdAt |
|---|---|---|---|---|---|---|---|---|

- `date`: `YYYY-MM-DD` 형식
- `period`: `1`~`8`, `4A`(4교시 저학년), `4E`(4교시 고학년), `5EH`(5교시). 정확한 키는 `app.js`의 `PERIODS` 배열 참조
- `classroom`: `"3학년 5반"` 형식, 또는 회의·행사 예약 시 `"회의/행사"`
- `passwordHash`: 미사용 필드(스키마만 유지, 항상 빈 값)
- `createdAt`: 생성 시각(ISO 형식)

### 4.2 `rooms` — 특별실

| name | order |
|---|---|

- `order`: 화면 탭 표시 순서

### 4.3 `dateRules` — 기간 정규시간 및 예약 금지

| id | room | startDate | endDate | periods | daysOfWeek | label | blocked |
|---|---|---|---|---|---|---|---|

- 정규시간(라벨) 정보는 전량 이 테이블에서 관리합니다.
- `periods`, `daysOfWeek`: JSON 문자열로 저장(예: `["1","2"]`, `["월","수"]`). 서버가 조회 시 배열로 복원
- `blocked`: `'true'` / `'false'` 문자열로 저장. `true`인 경우 해당 칸은 예약이 금지됨

### 4.4 `holidays` — 학교자체 휴일

| id | startDate | endDate | label |
|---|---|---|---|

- 방학, 재량휴업일, 개교기념일 등을 등록합니다.
- 법정공휴일은 이 테이블이 아닌 `app.js`의 `KR_HOLIDAYS` 배열에 정의되어 있으며, 현재 2026~2028년분이 등록되어 있습니다.

### 4.5 `logs` — 변경 기록

| ts | action | summary |
|---|---|---|

- 예약·기간 정규시간·특별실·휴일이 삭제 또는 수정되거나 라벨이 가려진 경우 한 행씩 기록합니다.
- 변경 주체(누가)는 기록하지 않으며, 변경 대상(무엇)만 기록합니다.
- 최대 3,000행 보관하며, 초과 시 오래된 행부터 삭제됩니다.

### 4.6 `schedule` — 미사용 탭

| room | dayOfWeek | period | label |
|---|---|---|---|

- 2026-08-28에 요일별 고정 정규시간 기능이 폐지되었습니다. 탭과 컬럼은 남아 있으나 애플리케이션에서 사용하지 않습니다.

---

## 5. 동작 방식

### 5.1 서버 통신

- **조회**: `GET .../exec?action=loadAll` 요청 시 전체 데이터를 JSON으로 반환합니다.
- **변경**: `POST .../exec` 요청에 `{"action":"create", ...}` 형식의 본문을 전달합니다. Content-Type 헤더를 생략하여 CORS 제약을 회피합니다.
- 서버의 `doGet` / `doPost` 함수가 `action` 값에 따라 처리를 분기합니다.

### 5.2 실시간 저장

과거에는 정규시간·특별실 정보를 별도 버튼으로 일괄 저장하는 방식이었으나, 데이터가 완전히 로드되지 않은 상태에서 저장할 경우 서버 데이터가 손실되는 사고(2026-08-27)가 발생하여, 모든 변경을 건별로 즉시 저장하는 방식으로 전환하였습니다.

| 데이터 | 저장 시점 | 서버 처리 |
|---|---|---|
| 예약 | 생성·수정·삭제 즉시 | `create` / `update` / `delete` |
| 특별실 | 추가·삭제·순서 변경 즉시 | `addRoom` / `deleteRoom` / `reorderRooms` |
| 기간 정규시간 | 추가·수정·삭제 즉시 | `createDateRule` / `updateDateRule` / `deleteDateRule` |
| 학교휴일 | 추가·수정·삭제 즉시 | `createHoliday` / `updateHoliday` / `deleteHoliday` |

- 별도의 저장 버튼은 존재하지 않으며, 모든 변경이 자동 저장됩니다.
- 서버는 각 요청을 `LockService`로 잠근 후 처리하므로, 동일한 시간대를 두 사용자가 동시에 예약하면 한 명만 성공합니다.
- 서버 저장이 실패하면 화면을 이전 상태로 되돌리고 사용자에게 알립니다.

### 5.3 자동 새로고침

| 모드 | 동작 |
|---|---|
| 사용자 모드 | 60초 간격으로 서버 데이터를 재조회하여 화면을 갱신합니다(타인의 예약 반영). |
| 관리자 모드 | 편집 중 화면 변동을 방지하기 위해 자동 조회를 중지합니다. 단, 중지되는 것은 조회뿐이며, 관리자의 변경 사항은 관리자 모드에서도 즉시 서버로 전송됩니다. |
| 최초 접속 시 | 모드와 무관하게 1회 전체 데이터를 조회합니다. |

### 5.4 관리자 인증

- 관리자 비밀번호는 브라우저 코드에 포함되지 않습니다. `Code.gs`의 `ADMIN_PW` 값에만 존재하며, 서버가 `checkAdmin` 요청으로 검증합니다.
- 서버 연동이 비활성화된 경우 관리자 모드에 진입할 수 없습니다.

### 5.5 연결 설정 자동 적용

`config.js`의 `version` 값이 변경되면, 기존에 접속한 적이 있는 브라우저에도 새로운 `apiUrl` 등의 설정이 자동 적용됩니다. 담당자 인수인계 시 이 방식으로 새 시트에 연결합니다(제6장 참조).

---

## 6. 기능 목록

### 6.1 일반 사용자 기능

| 기능 | 설명 |
|---|---|
| 주간 시간표 | 월~금 × 1~8교시(4교시는 저·고학년 2칸으로 분리). 달력을 통해 임의 날짜로 이동 |
| 월별 보기 | 달력 형태로 각 날짜의 예약을 "교시 학급" 형식으로 표시 |
| 예약 | 빈 칸 선택 → 이름·학급(학년/반, 12반까지)·목적 입력. 회의·행사 선택 시 학년/반 입력 생략. 이름·학급은 브라우저에 기억되어 다음 예약 시 자동 입력 |
| 예약 수정·삭제 | 비밀번호 확인 없음(정책상 의도된 사항) |
| 다중 선택 | 여러 빈 칸을 한 번에 예약하거나, 여러 예약을 한 번에 삭제(삭제 시 대상 목록을 확인창에 표시) |
| 내 예약 조회 | 이름으로 검색 |
| 기간 정규시간 편성 | 특별실·기간·요일·교시별로 라벨을 설정하거나 예약을 금지 |
| 특별실 추가 | 일반 사용자도 가능(삭제·순서 변경은 관리자 전용) |

법정공휴일 및 학교자체 휴일은 회색으로 표시되며, 해당일에도 예약은 가능합니다.

### 6.2 관리자 기능 (일반 기능에 더하여)

| 기능 | 설명 |
|---|---|
| 관리자 모드 진입 | 화면 우측 상단 버튼 → 비밀번호 입력(서버 검증) |
| 특별실 삭제 | 탭 우클릭. 해당 특별실의 예약·기간 규칙도 함께 삭제되며, 확인창에 건수를 표시 |
| 특별실 순서 변경 | 탭을 약 0.35초 이상 누른 후 좌우로 이동 |
| 학교휴일 관리 | 방학·재량휴업일 등의 등록·수정·삭제 |
| 변경 기록 조회 | 삭제·수정·가림된 내용을 최근 순으로 확인 |
| 구글시트 설정 | 서버 연동 URL, 자동 저장·조회 설정, 서버 데이터 정리(중복 제거), 브라우저 데이터 초기화 |
| 서버에서 불러오기 | 서버 데이터로 현재 화면을 강제 갱신 |

### 6.3 예약 금지 규칙의 동작

- 예약 금지를 설정하려는 기간·교시에 이미 예약이 존재하는 경우, 해당 예약을 삭제하지 않고 목록만 표시한 후 처리를 중단합니다. 예약자에게 직접 취소를 요청한 뒤 다시 시도해야 합니다.
- 여러 규칙이 겹치는 경우, 그중 하나라도 예약 금지이면 예약 금지가 우선하여 표시됩니다.
- 규칙은 서로 덮어써도 삭제되지 않으므로, 상위 규칙을 삭제하면 하위 규칙이 다시 표시됩니다.

---

## 7. 신규 구축 절차 (타 학교 도입 시)

### 7.1 소스 코드 준비

```bash
git clone https://github.com/shinkim0712/specialroom.git
cd specialroom
```

### 7.2 데이터베이스 및 서버 구성 (Google)

1. Google Drive에서 새 스프레드시트를 생성합니다(이름 임의).
2. 주소창에서 시트 ID를 확인합니다: `https://docs.google.com/spreadsheets/d/【시트 ID】/edit`
3. 스프레드시트 메뉴에서 **확장 프로그램 → Apps Script**를 선택합니다.
4. 편집기의 기존 내용을 모두 삭제하고 `apps-script/Code.gs`의 내용을 붙여넣습니다.
5. 코드 상단의 두 값을 수정합니다.
   ```js
   const SHEET_ID = '2번에서 확인한 시트 ID';
   const ADMIN_PW = '설정할 관리자 비밀번호';
   ```
6. **배포 → 새 배포 → 유형: 웹앱**을 선택합니다.
   - 실행 계정: **본인**
   - 액세스 권한: **모든 사용자**
   - 배포 후 권한을 승인하고, 발급된 `https://script.google.com/macros/s/.../exec` 주소를 복사합니다.
7. 발급된 주소 뒤에 `?action=ping`을 붙여 브라우저로 접속하여 `{"ok":true, ...}` 응답을 확인합니다.

### 7.3 서버 연결 설정

`config.js`를 수정합니다.
```js
const APP_CONFIG = {
  version: 3,          // 기존 값에서 1 증가
  apiUrl: 'https://script.google.com/macros/s/.../exec',   // 7.2에서 발급받은 주소
  serverEnabled: true,
  autoSave: true,
  autoLoad: true,
  autoLoadInterval: 60,
};
```
`index.html`의 `<script src="config.js?v=2">`를 `config.js?v=3`으로 수정하여 버전을 일치시킵니다.

### 7.4 사이트 배포 (GitHub Pages)

```bash
git remote set-url origin https://github.com/【학교 계정】/specialroom.git
git add -A && git commit -m "학교별 설정" && git push -u origin main
```
GitHub 저장소에서 **Settings → Pages → Source: `main` / `/(root)` → Save**를 설정합니다.
1~2분 후 `https://【계정】.github.io/specialroom/`로 접속할 수 있으며, 이 주소를 교직원에게 공유합니다.

### 7.5 초기 데이터 입력

관리자 모드에서 다음을 등록합니다.
- 특별실 목록 (특별실 추가 기능)
- 정규시간표 (기간 정규시간 기능)
- 학교자체 휴일 (학교휴일 관리 기능)

### 7.6 연도별 법정공휴일 갱신

`app.js`의 `KR_HOLIDAYS` 배열에는 2028년분까지 등록되어 있습니다. **2029년 이전에 해당 연도의 공휴일을 배열에 추가**하고 `app.js`의 `?v=` 번호를 증가시킨 후 반영해야 합니다.

---

## 8. 유지보수 절차

### 8.1 클라이언트(HTML/CSS/JavaScript) 수정

```bash
cd specialroom
# 파일 수정
# 수정한 파일에 대응하는 index.html의 ?v= 번호를 1 증가
git add -A && git commit -m "수정 내용" && git push
# 1~2분 후 GitHub Pages·Vercel에 자동 반영됨
```

로컬 미리보기:
```bash
python3 -m http.server 8123 --directory specialroom
# http://localhost:8123 접속
```
서버 기능 테스트 시에는 반드시 테스트 전용 시트를 사용합니다(제9장 참조).

### 8.2 서버(Code.gs) 수정

소스 반영과 별개로 Apps Script에 직접 적용해야 합니다.
1. `apps-script/Code.gs`를 수정합니다.
2. Apps Script 편집기에서 전체 선택 → 삭제 → 붙여넣기를 수행합니다.
3. 상단의 `SHEET_ID` / `ADMIN_PW` 두 줄을 해당 환경의 값으로 다시 수정합니다.
4. 저장합니다(Ctrl/Cmd + S).
5. **배포 → 배포 관리 → 편집 → 버전을 "새 버전"으로 선택 → 배포**합니다.
   - "새 버전"을 선택하지 않으면 코드가 반영되지 않습니다(가장 빈번한 오류).
   - "새 배포"를 수행하면 주소가 변경되므로 사용하지 않습니다.

---

## 9. 테스트 규칙

본 시스템은 학교 현장에서 상시 운영 중이므로, 잘못된 조작 시 실제 예약이 손실될 수 있습니다. 실제로 2026-08-27 기능 검증 과정에서 예약 1건과 학교휴일 2건이 삭제된 사례가 있었습니다.

이에 따라 **운영 환경과 완전히 분리된 테스트 전용 구글시트 및 Apps Script**를 별도로 구성해 두었습니다.

- 브라우저에서 기능을 검증할 때는 개발자 콘솔에서 다음을 먼저 실행합니다.
  ```js
  localStorage.setItem('apiUrl', '테스트 Apps Script 주소');
  location.reload();
  ```
- 검증 종료 후 `localStorage.removeItem('apiUrl'); location.reload();`로 운영 환경에 복귀합니다.
- 운영 서버에는 조회(`ping`, `loadAll`) 요청만 수행하며, 변경 요청 테스트는 수행하지 않습니다.
- 테스트 환경의 주소는 `CONTEXT.md` 상단에 기재되어 있습니다.

---

## 10. 인수인계 절차 (담당자 변경 시)

### 10.1 재구성이 필요한 이유

서버(Apps Script 웹앱)는 최초 배포자의 Google 계정 권한으로 실행됩니다. 해당 계정이 전보·퇴직 등으로 정지되면 서버가 중단되므로, 담당자 변경 시 **후임자의 계정으로 서버를 새로 배포**해야 합니다.

### 10.2 기본 절차 (기존 예약 데이터를 이관하지 않는 경우)

1. 후임 담당자가 본인 Google Drive에 새 스프레드시트를 생성합니다.
2. 확장 프로그램 → Apps Script에서 `apps-script/Code.gs`의 내용을 붙여넣습니다.
3. 상단 두 줄을 수정합니다: `SHEET_ID`(신규 시트), `ADMIN_PW`(신규 비밀번호).
4. 배포 → 새 배포 → 웹앱(실행: 본인 / 액세스: 모든 사용자) → 발급된 `.../exec` 주소를 복사합니다.
5. `config.js`를 수정합니다: `apiUrl`을 신규 주소로 교체, `version`을 1 증가.
6. `index.html`의 `config.js?v=N`도 동일하게 증가시킵니다.
7. `git add -A && git commit -m "담당자 인수인계" && git push`
8. 1~2분 후 모든 사용자 브라우저가 자동으로 새 시트에 연결됩니다(사용자의 별도 조치 불필요).

### 10.3 신규 입력이 필요한 항목 (데이터 미이관 시)

- 특별실 목록 (특별실 추가 기능)
- 정규시간표 (기간 정규시간 기능)
- 학교자체 휴일 (학교휴일 관리 기능)

### 10.4 기존 데이터를 이관하는 경우 (선택)

전임 담당자의 협조가 가능한 경우, 기존 스프레드시트를 **파일 → 사본 만들기**로 후임 담당자 Drive에 복사한 뒤, 해당 사본의 ID를 새 `Code.gs`의 `SHEET_ID`에 입력하면 예약 및 정규시간 데이터가 그대로 유지됩니다.

### 10.5 테스트 환경 이관

`CONTEXT.md`에 기재된 테스트 전용 시트 및 Apps Script도 후임 담당자 계정으로 재구성해 두는 것을 권장합니다.

---

## 11. 알려진 제약 사항

| 항목 | 내용 |
|---|---|
| 특별실 삭제 시 건수 표시 | 관리자 모드는 자동 새로고침이 중지되므로, 삭제 확인창에 표시되는 "예약 N건 함께 삭제" 수치가 최신이 아닐 수 있습니다. 삭제 전 사용자 모드로 전환하거나 "서버에서 불러오기"로 갱신할 것을 권장합니다. |
| 학교휴일 기간 중복 | 등록 시 기간 중복을 검증하지 않습니다. |
| 예약 금지 차단 범위 | 클라이언트 수준에서만 차단하며, 서버 API를 직접 호출하는 경우는 차단하지 않습니다(일반 사용 시 문제 없음). |
| 브라우저 저장 용량 | 예약이 수천 건 이상 누적되면 브라우저 `localStorage` 용량 한계에 도달할 수 있습니다. 오래된 예약의 주기적 정리를 권장합니다. |
| HTTPS 필요 | 예약 ID 생성에 사용하는 `crypto.randomUUID`는 HTTPS 환경에서만 동작합니다. GitHub Pages·Vercel은 HTTPS를 제공하므로 정상 동작하며, HTTP로 접속할 경우 예약 생성이 되지 않습니다. |
| 민감 정보 노출 | `config.js` 및 소스 저장소의 문서에 시트 ID와 Apps Script 주소가 포함되어 있습니다. Apps Script 주소는 원래 공개(모든 사용자 접근 허용) 대상이므로 문제가 없으나, 저장소를 공개로 유지할 경우 추가적인 민감 정보를 포함하지 않아야 합니다. 관리자 비밀번호(`ADMIN_PW`)는 `.gitignore`로 제외된 `Code.gs`에만 존재하며 저장소에 포함되지 않습니다. 이 상태를 유지해야 합니다. |
