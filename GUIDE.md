# 특별실 예약 관리 웹앱 — 전체 안내서

이 문서 하나로 **이 앱이 어떻게 만들어졌고 / 어떤 기능이 있고 / 다른 학교가 따라 만들려면 / 관리자가 바뀌면 어떻게 하는지**를 전부 설명합니다.

- 실제 개발용 상세 메모: [`CONTEXT.md`](CONTEXT.md)
- 버전별 변경 내역: [`CHANGELOG.md`](CHANGELOG.md)
- 짧은 요약: [`README.md`](README.md)

---

## 1. 이게 뭔가

학교 특별실(강당·운동장·시청각실·도서관·컴퓨터실 등)을 선생님들이 브라우저로 예약하는 웹앱입니다.

- **서버 비용 0원.** 구글이 무료로 주는 것만 씀 (Apps Script + Sheets + GitHub Pages)
- **로그인 없음.** 이름만 적으면 예약. 학교 내부에서만 URL 공유해서 씀
- **타깃 사용자는 4~50대 비전문가 교사.** 그래서 UI는 색·기호보다 **읽으면 바로 이해되는 텍스트** 위주

### 지금 운영 상황 (2026-08-28 기준)

| 항목 | 내용 |
|---|---|
| 상태 | 실제 학교에서 운영 중 |
| 배포 주소 | GitHub Pages + Vercel (둘 다 같은 코드, push하면 자동 재배포) |
| GitHub | `shinkim0712/specialroom` |
| 데이터 규모 | 특별실 9개, 기간 정규시간 100건+, 예약 상시 |
| 서버 실행 계정 | 배포한 사람의 개인 구글 계정 (→ 6번 인수인계 참고) |

---

## 2. 전체 구조 (한눈에)

```
┌─────────────────┐   ①화면 열기        ┌──────────────────────┐
│  선생님 브라우저  │ ─────────────────▶ │  GitHub Pages/Vercel   │
│  (HTML/CSS/JS)   │ ◀───────────────── │  (정적 파일만 호스팅)   │
└────────┬────────┘   HTML/CSS/JS 받음   └──────────────────────┘
         │
         │ ②예약 저장/불러오기 (fetch, JSON)
         ▼
┌─────────────────────┐   ③시트 읽기/쓰기   ┌──────────────────┐
│  Google Apps Script  │ ─────────────────▶ │  Google Sheets    │
│  (Code.gs = 서버 역할) │ ◀───────────────── │  (DB 역할, 탭 6개)  │
└─────────────────────┘                    └──────────────────┘
```

- **프론트엔드**: 순수 HTML/CSS/JavaScript. 프레임워크 없음, 빌드 과정 없음. 파일 그대로 올림
- **호스팅**: GitHub Pages(주), Vercel(보조). 둘 다 "정적 파일 서버"일 뿐, 아무 로직도 안 돌림
- **서버**: Google Apps Script 웹앱 1개(`Code.gs`). `/exec` URL로 GET/POST를 받아 시트를 조작
- **DB**: Google Sheets 1개. 탭이 곧 테이블

### 오프라인에서도 동작

브라우저는 모든 데이터를 `localStorage`에도 저장합니다. 서버가 죽어도 **내 브라우저에 마지막으로 본 상태**는 계속 보이고, 예약도 로컬엔 저장됩니다 (서버 복구되면 자동 동기화). 서버 연동을 아예 꺼도 1인용 앱처럼 동작합니다.

---

## 3. 파일별 설명

```
specialroom/
├── index.html      화면 뼈대 + 모든 모달(팝업). <script>로 config→api→app 순서 로드
├── style.css        디자인 (Slate Blue #0e3b69 포인트, 미니멀)
├── config.js        ★ 구글시트 연결 설정. 여기 apiUrl만 바꾸면 다른 시트로 연결됨
├── api.js           Apps Script와 통신하는 함수 모음 (fetch 래퍼)
├── app.js           ★ 전체 클라이언트 로직 (~1400줄, 제일 큼)
├── apps-script/
│   └── Code.gs       ★ 서버 코드. Apps Script 편집기에 붙여넣는 용도
│                     (.gitignore로 제외됨 — 시트ID·관리자비번이 들어있어서)
├── .claude/launch.json   로컬 프리뷰 설정 (python http.server 8123)
├── README.md / CONTEXT.md / CHANGELOG.md / GUIDE.md(이 문서)
```

### 캐시 버스팅 (중요)

`index.html`이 `style.css?v=39`, `app.js?v=57`, `api.js?v=22`, `config.js?v=2` 처럼 버전 쿼리를 붙여 파일을 부릅니다.

> **CSS/JS를 고칠 때마다 그 파일의 `?v=` 숫자를 반드시 +1** 해야 브라우저가 새 파일을 받아옵니다. 안 올리면 캐시된 옛 파일이 계속 로드돼서 "고쳤는데 왜 그대로지?"가 됩니다. (여러 번 겪은 문제)

---

## 4. 데이터 모델 (Google Sheets 탭)

Code.gs가 처음 호출될 때 없는 탭은 자동 생성합니다. 직접 만들 필요 없음.

### `reservations` — 예약
| id | room | date | period | name | classroom | purpose | passwordHash | createdAt |
|---|---|---|---|---|---|---|---|---|

- `date`: `YYYY-MM-DD`
- `period`: `1`~`8`, `4A`(4교시 저학년), `4E`(4교시 고학년), `5EH`(5교시). 정확한 키는 `app.js`의 `PERIODS` 배열
- `classroom`: `"3학년 5반"` 또는 `"회의/행사"` (회의/행사 체크 시)
- `passwordHash`: **죽은 필드** (스키마만 남음, 항상 빈 값)
- `createdAt`: ISO 타임스탬프

### `rooms` — 특별실
| name | order |
|---|---|

- `order`: 탭에 표시되는 순서

### `dateRules` — 기간 정규시간 / 예약 금지 (★ 정규시간은 전부 여기)
| id | room | startDate | endDate | periods | daysOfWeek | label | blocked |
|---|---|---|---|---|---|---|---|

- `periods`, `daysOfWeek`: JSON 문자열 (예: `["1","2"]`, `["월","수"]`). Code.gs가 읽을 때 배열로 복원
- `blocked`: `'true'`/`'false'` 문자열로 저장, boolean으로 복원. `true`면 그 칸은 예약 불가

### `holidays` — 학교자체 휴일 (방학·재량휴업일 등)
| id | startDate | endDate | label |
|---|---|---|---|

- 법정공휴일은 여기 없음 — `app.js`의 `KR_HOLIDAYS` 배열에 하드코딩 (2026~2028년치 등록됨)

### `schedule` — 죽은 탭
| room | dayOfWeek | period | label |
|---|---|---|---|

- 2026-08-28 "요일별 고정 정규시간" 기능 제거. 탭·컬럼은 남아있지만 앱이 안 씀

### `logs` — 변경 기록 (2026-08-28 추가)
| ts | action | summary |
|---|---|---|

- 예약·기간규칙·특별실·휴일이 **삭제·수정됐거나 라벨이 가려졌을 때** 한 줄씩 기록
- **누가 했는지는 안 남김** — "무엇이 없어졌나"만
- 3000줄까지 보관, 넘으면 오래된 것부터 삭제

---

## 5. 동작 원리

### 5-1. 서버 통신 (api.js ↔ Code.gs)

- **읽기**: `GET .../exec?action=loadAll` → 전체 데이터 JSON
- **쓰기**: `POST .../exec` + body `{"action":"create", ...}` (Content-Type 헤더 생략 = CORS 회피 트릭)
- Code.gs의 `doGet`/`doPost`가 `action`으로 분기

### 5-2. 실시간 낱개 저장 (2026-08-28~)

**예전엔** 정규시간·특별실을 "정규시간표 서버저장" 버튼으로 통째 덮어썼습니다. 로딩이 덜 된 상태로 누르면 서버 데이터가 날아가는 사고가 있어서(2026-08-27), **전부 "한 건씩 즉시 저장"으로 바꿨습니다.**

| 데이터 | 저장 시점 | 서버 액션 |
|---|---|---|
| 예약 | 만들/고칠/지울 때 즉시 | `create` / `update` / `delete` |
| 특별실 | 추가·삭제·순서변경 시 즉시 | `addRoom` / `deleteRoom` / `reorderRooms` |
| 기간 정규시간 | 추가·수정·삭제 시 즉시 | `createDateRule` / `updateDateRule` / `deleteDateRule` |
| 학교휴일 | 추가·수정·삭제 시 즉시 | `createHoliday` / `updateHoliday` / `deleteHoliday` |

- **"저장" 버튼은 이제 없습니다.** 모든 게 자동
- 서버가 각 요청을 `LockService`로 잠그고 처리 → 두 사람이 같은 칸을 동시에 예약해도 한 명만 성공
- 서버 저장이 실패하면 화면을 되돌리고 알림 (예약·기간규칙·휴일)

### 5-3. 자동 불러오기

- **사용자 모드**: 60초마다 서버에서 전체를 다시 받아와 화면 갱신 (남의 예약 반영)
- **관리자 모드**: 자동 불러오기 **정지** (편집 중 화면이 튀지 않도록). 단 정지되는 건 "받아오는 쪽"뿐 — 관리자가 바꾼 건 관리자 모드에서도 즉시 서버로 나감
- **페이지 처음 열 때**: 모드 무관 1회 전체 최신화

### 5-4. 관리자 인증

- 비밀번호는 **브라우저 코드에 없음**. `Code.gs`의 `ADMIN_PW`에만 있고 서버가 `checkAdmin` 액션으로 검증
- 서버 연동이 꺼져 있으면 관리자 모드 진입 불가

### 5-5. config.js 자동 적용

`config.js`의 `version`이 바뀌면, 이미 사이트를 써본 브라우저에도 새 `apiUrl` 등이 자동 적용됩니다 (localStorage에 `configVersion` 저장해서 비교). → 관리자 인수인계 때 이 방식으로 새 시트에 연결.

---

## 6. 기능 목록

### 일반 사용자

- **주간 시간표**: 월~금 × 1~8교시 (4교시는 저/고학년 2칸). flatpickr 캘린더로 임의 날짜 이동
- **월별 보기**: 달력 형태, 각 날짜에 예약을 "교시 학급" 텍스트로 나열
- **예약**: 빈 칸 클릭 → 이름·학급(학년/반, 12반까지)·목적 입력. "회의/행사" 체크 시 학년/반 숨김
  - 이름·학급은 브라우저에 기억되어 다음 예약에 자동 채워짐 ("자동입력 지우기"로 삭제 가능)
- **예약 수정·삭제**: 비밀번호 없음 (의도적 정책)
- **다중 선택**: 빈 칸 여러 개 일괄 예약 / 예약된 칸 여러 개 일괄 삭제 (삭제 확인창에 목록 표시)
- **내 예약 보기**: 이름으로 검색
- **기간 정규시간 편성**: 특별실·기간·요일·교시로 라벨 설정 또는 "예약 금지". 사용자도 가능
- **특별실 추가**: 사용자도 가능 (삭제·순서변경은 관리자만)
- 법정공휴일·학교휴일은 회색으로 표시 (그날도 예약은 가능)

### 관리자 (사용자 기능 + 아래)

- **관리자 모드 진입**: 우측 상단 버튼 → 비밀번호 (서버 검증)
- **특별실 삭제**: 탭 우클릭. 그 특별실의 예약·기간규칙도 함께 삭제됨 (확인창에 건수 표시)
- **특별실 순서 변경**: 탭을 0.35초 꾹 눌렀다가 좌우로 드래그
- **학교휴일 관리**: 방학·재량휴업일 등 등록/수정/삭제
- **변경 기록**: 삭제·수정·가림된 내용을 최근순으로 확인
- **구글시트 설정**: 서버 URL, 자동저장/불러오기 토글, "서버 데이터 정리"(중복 제거), "이 PC 데이터 초기화"
- **서버에서 불러오기**: 서버 데이터로 내 화면을 강제 교체

### 예약 금지 규칙의 동작

- 예약 금지를 걸려는데 **이미 예약이 있으면** → 삭제하지 않고 그 예약 목록만 보여준 뒤 중단. 예약자에게 직접 연락해 지운 뒤 다시 시도
- 겹친 규칙 중 **하나라도 예약금지면** 무조건 예약금지가 화면에 우선 표시 (🔒 빨강)
- 규칙은 서로 덮어써도 지워지지 않음 — 위엣것을 삭제하면 아래 것이 다시 보임

---

## 7. 다른 학교가 처음부터 만드는 법

### 7-1. 코드 준비

```bash
# 이 저장소를 그대로 복사 (fork 또는 다운로드)
git clone https://github.com/shinkim0712/specialroom.git
cd specialroom
```

### 7-2. 구글 시트 + Apps Script (서버)

1. drive.google.com → 새 스프레드시트 하나 만들기 (이름 자유)
2. URL에서 시트 ID 복사: `https://docs.google.com/spreadsheets/d/【이 부분】/edit`
3. 시트에서 **확장 프로그램 → Apps Script**
4. 편집기 내용 전부 지우고 `apps-script/Code.gs` 내용 붙여넣기
5. 코드 맨 위 두 줄 수정:
   ```js
   const SHEET_ID = '위에서 복사한 시트 ID';
   const ADMIN_PW = '원하는 관리자 비밀번호';
   ```
6. **배포 → 새 배포 → 유형: 웹앱**
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자**
   - 배포 → 권한 승인 → `https://script.google.com/macros/s/.../exec` URL 복사
7. 확인: 그 URL 뒤에 `?action=ping` 붙여 브라우저로 열기 → `{"ok":true,...}` 나오면 성공

### 7-3. 코드에 서버 연결

`config.js` 수정:
```js
const APP_CONFIG = {
  version: 3,          // 기존 숫자에서 +1
  apiUrl: 'https://script.google.com/macros/s/.../exec',   // 7-2에서 받은 URL
  serverEnabled: true,
  autoSave: true,
  autoLoad: true,
  autoLoadInterval: 60,
};
```
`index.html`에서 `<script src="config.js?v=2">` → `config.js?v=3` (버전 맞추기)

### 7-4. GitHub Pages 배포

```bash
git remote set-url origin https://github.com/【우리학교아이디】/specialroom.git
git add -A && git commit -m "우리 학교용 설정" && git push -u origin main
```
GitHub 저장소 → **Settings → Pages → Source: `main` / `/(root)` → Save**
1~2분 뒤 `https://【아이디】.github.io/specialroom/` 접속 가능. 이 주소를 교직원에게 공유.

### 7-5. 초기 데이터 입력

관리자 모드로 들어가서:
- 특별실 추가 ("+ 특별실 추가")
- 정규시간표 → "기간 정규시간" 버튼으로 요일·교시별 라벨 등록
- 학교휴일 → "학교휴일 관리"로 방학 등 등록

### 7-6. 다음 해 공휴일

`app.js`의 `KR_HOLIDAYS` 배열은 2028년까지만 들어있습니다. **2029년이 되기 전에 새 연도 공휴일을 배열에 추가**하고 `app.js?v=` 숫자를 올려 push.

---

## 8. 유지보수 (코드 고칠 때)

### 클라이언트 (HTML/CSS/JS) 수정

```bash
cd specialroom
# 파일 수정...
# ★ 고친 파일의 index.html 안 ?v= 숫자를 +1
git add -A && git commit -m "수정 내용" && git push
# → 1~2분 뒤 GitHub Pages·Vercel 자동 재배포
```

로컬에서 미리 보기:
```bash
python3 -m http.server 8123 --directory specialroom
# http://localhost:8123 접속
```
> ⚠️ 로컬에서 서버 기능을 테스트하려면 **테스트 전용 시트**를 써야 합니다 (아래 9번).

### 서버 (Code.gs) 수정

`git push`와 별개로 Apps Script에 직접 반영해야 합니다:
1. `apps-script/Code.gs` 수정
2. Apps Script 편집기에서 **전체 선택 → 삭제 → 붙여넣기**
3. 맨 위 `SHEET_ID` / `ADMIN_PW` 두 줄을 **원래 값으로 다시 교체** (붙여넣은 건 플레이스홀더)
4. Cmd/Ctrl+S 저장
5. **배포 → 배포 관리 → 편집(연필) → 버전 "새 버전" 선택 → 배포**
   - ⚠️ "새 버전"을 안 고르면 코드가 안 바뀝니다 (가장 흔한 실수)
   - "새 배포"를 하면 URL이 바뀌므로 절대 하지 말 것

---

## 9. ⚠️ 테스트 규칙 (반드시)

이 앱은 실제 학교에서 운영 중이라, 잘못 건드리면 진짜 예약이 사라집니다. 실제로 2026-08-27에 검증 중 실수로 예약 1건·학교휴일 2건이 삭제된 사고가 있었습니다.

**그래서 운영과 완전히 분리된 "테스트 전용 구글시트 + Apps Script"를 따로 만들어뒀습니다.**

- 브라우저에서 기능 검증할 땐 콘솔에서 먼저:
  ```js
  localStorage.setItem('apiUrl', '테스트 Apps Script URL')
  location.reload()
  ```
- 끝나면 `localStorage.removeItem('apiUrl'); location.reload()` 로 운영 복귀
- 운영 서버는 **읽기(`ping`, `loadAll`)만** — 쓰기 테스트는 절대 운영에서 하지 말 것
- 테스트 URL은 `CONTEXT.md` 상단 참고

---

## 10. 인수인계 (관리자·담당 교사가 바뀔 때)

### 왜 새로 연결해야 하나

서버(Apps Script 웹앱)는 **배포한 사람의 구글 계정 권한으로 실행**됩니다. 그 계정이 전근·퇴직으로 정지되면 서버가 멈춥니다. 그래서 담당자가 바뀌면 **새 담당자 계정으로 서버를 새로 배포**해야 합니다.

### 절차 (지난 예약 기록은 안 넘어온다는 전제 — 가장 단순)

1. **새 담당자**가 자기 구글 드라이브에 새 스프레드시트 생성
2. 확장 프로그램 → Apps Script → 이 저장소의 `apps-script/Code.gs` 붙여넣기
3. 맨 위 두 줄 수정: `SHEET_ID`(새 시트), `ADMIN_PW`(새 비번)
4. 배포 → 새 배포 → 웹앱 (실행: 나 / 액세스: 모든 사용자) → `.../exec` URL 복사
5. 이 저장소 `config.js` 수정: `apiUrl`을 새 URL로, `version` +1
6. `index.html`의 `config.js?v=N`도 +1
7. `git add -A && git commit -m "관리자 인수인계" && git push`
8. 1~2분 뒤 모든 사용자 브라우저가 자동으로 새 시트에 연결됨 (사용자는 아무것도 안 해도 됨)

### 새로 입력해야 하는 것 (기존 데이터는 안 넘어옴)

- 특별실 목록 → "+ 특별실 추가"
- 정규시간표 → "기간 정규시간" 버튼
- 학교휴일 → "학교휴일 관리"

### 데이터를 이어받고 싶으면 (선택)

기존 담당자가 협조 가능하면, 기존 구글시트를 **파일 → 사본 만들기**로 새 담당자 드라이브에 복사한 뒤, 그 사본의 ID를 새 `Code.gs`의 `SHEET_ID`에 넣으면 예약·정규시간이 그대로 이어집니다.

### 테스트 시트도 함께 넘길 것

`CONTEXT.md`에 적힌 테스트 전용 시트/Apps Script도 새 담당자 계정으로 다시 만들어 두면 안전합니다.

---

## 11. 알려진 제약 / 주의사항

- **관리자 모드에서 특별실 삭제 시**: 관리자 모드는 자동 새로고침이 꺼져 있어, 삭제 확인창의 "예약 N건 함께 삭제" 숫자가 낡았을 수 있습니다. 삭제 전에 잠깐 사용자 모드로 나갔다 들어오거나 "서버에서 불러오기"로 최신화하는 걸 권장
- **학교자체 휴일은 기간 겹침 검증 없음**
- **예약 금지는 클라이언트 차단만** (서버 API를 직접 호출하는 건 안 막음 — 일반 사용 시 문제 없음)
- **예약이 몇 년치 쌓이면** 브라우저 localStorage 용량 한계에 닿을 수 있음 (수천 건 이상). 오래된 예약은 주기적으로 정리 권장
- **`crypto.randomUUID`는 https에서만 동작** — GitHub Pages/Vercel은 https라 정상. http로 접속하면 예약 생성이 안 됨
- **`config.js`·`apps-script/Code.gs`의 시트 ID / Apps Script URL은 공개 저장소에 노출됨.** Apps Script URL은 원래 공개(모든 사용자 접근)라 문제없지만, 저장소를 공개로 둘 거면 민감 정보를 더 넣지 말 것. `ADMIN_PW`는 `.gitignore`된 `Code.gs`에만 있고 git에 안 올라감 — 이 상태 유지
