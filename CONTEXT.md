# 특별실 예약 관리 웹앱 — 컨텍스트

> 이 문서는 세션이 바뀌어도 이어서 작업할 수 있도록 정리한 현재 상태 스냅샷입니다.
> 마지막 갱신: 2026-08-28

## ✅ 2026-08-28 전체 배포 완료
- 1차: 실시간 동기화, 요일별 정규시간 제거, 변경 기록 등 (커밋 `c7b9c64`~`2aacb83`)
- 2차: 학교휴일 실시간화, `saveAll` 제거, `deleteRoom` 행단위 삭제(#2), 다중삭제 확인창 상세화 (커밋 `0346124`)
- 클라이언트 push 완료, 운영·테스트 Apps Script 배포 완료
- 운영 확인: `?action=logs` → `[]`, `saveAll` → `unknown action`, 데이터 무손실(rooms 9/res 11/rules 101/hols 0)
- app.js v57 / api.js v22
- Code.gs 새 액션: `createHoliday` `updateHoliday` `deleteHoliday`. `saveAll` 액션·함수 **완전 제거**
- 클라이언트: **"저장" 버튼 삭제** (학교휴일이 실시간 저장으로 바뀌어 버튼 불필요). `state.serverEverLoaded` 제거
- 다중선택 일괄삭제 확인창이 삭제될 예약 목록(방·날짜·교시·이름·학급)을 최대 15건 표시

### Code.gs 낱개 저장 액션 (전부 실시간, autoSave 게이트)
예약: `create`/`update`/`delete` · 특별실: `addRoom`/`deleteRoom`(cascade)/`reorderRooms`
기간정규시간: `createDateRule`/`updateDateRule`/`deleteDateRule` · 학교휴일: `createHoliday`/`updateHoliday`/`deleteHoliday`
로그: `log`(클라가 기록) / `logs`(읽기)
- **통째 덮어쓰기(saveAll) 경로는 완전히 제거됨.** 이제 "저장 버튼 눌러야 반영"되는 데이터 없음
- 삭제·수정 함수가 `logs` 시트에 "없어진 것" 기록 (appendLog, 3000줄 캡)

### 위험 분석 대응
- **#2 해결**: `deleteRoom`이 예약·정규시간·기간규칙을 `writeSheet`(clear+rewrite) 대신 `deleteRowsWhere`(해당 행만 역순 삭제)로 제거. 이제 "시트가 비는 순간"이 없어 중간에 실패해도 나머지 데이터 무사. `writeSheet`는 rooms(작음)·cleanup(드묾)에만 남음
- **#1 보류** (사용자 판단): 관리자 모드는 자동 불러오기가 꺼져 있어 특별실 삭제 확인창의 "예약 N건 함께 삭제"가 낡은 숫자일 수 있음. 최악의 경우 예약 많은 방을 "비었네" 하고 삭제. → 삭제 직전 서버 재조회하면 해결되지만 이번엔 안 함

### 이번 배치에서 한 것 (2026-08-28, 큰 묶음)
- 1~11: 기간규칙 사용자 개방+실시간, 특별실 추가 사용자 개방, 특별실 삭제/순서 실시간, "저장" 버튼 축소+안전장치, 예약금지 자동취소 폐지, 예약 유령버그, 특별실 삭제 cascade, 존댓말, 서버불러오기 관리자전용, 관리자 문구, Code.gs 보기 제거
- 12: **겹친 기간규칙 표시 버그** — 예약금지 칸에 일반 라벨을 얹으면 예약금지가 화면에서 사라지던 것. → 겹친 규칙 중 하나라도 `blocked`면 무조건 예약금지 우선
- A: 기간 정규시간 목록을 **선택한 특별실 것만** 표시 (`#drRoom` onchange)
- B: **요일별 정규시간(schedule) 기능 제거** — 운영 0건이었음. 관리자 빈 칸 클릭 = 예약(사용자와 동일), 다중선택도 예약, `scheduleLabelModal`·`editScheduleLabel`·정규시간 편집 버튼 삭제. `state.schedule`은 죽은 필드로 남음. "저장" 버튼은 이제 학교휴일만
- C: 예약금지 걸 때 그 칸에 정규시간 라벨이 있으면 "가려진다" 안내창
- D: 라벨 위에 라벨 얹을 때 "「기존라벨」이 가려진다" 안내창 (기존 라벨 이름 표시)
- E: **변경 기록** — `logs` 시트 + 관리자 "변경 기록" 버튼/모달. 삭제·수정·가림만 기록, 누가는 안 남김

## 🚨 무조건 지켜야 하는 규칙 — 테스트는 테스트 시트로만

**이 앱은 실제 학교에서 운영 중입니다.** 정규시간표(dateRules) 100건 이상, 여러 특별실, 실제 선생님 예약이 들어있습니다.

과거(2026-08-27) 기능 검증 중 실수로 **실제 예약 1건과 학교휴일 2건이 삭제**된 사고가 있었습니다 (아래 "사고 이력" 참고). 이후 재발 방지를 위해 **운영용과 완전히 분리된 테스트 전용 구글시트/Apps Script**를 만들어뒀습니다.

- **운영 서버(절대 함부로 쓰기 금지)**: `https://script.google.com/macros/s/AKfycbxYOrmLo9opdrbxXmCsWshDWfhtzDBFyAT2WIFOO-RZHMsMj73fPpgyNH7tbXb8JOY/exec`
- **테스트 서버(기능 검증은 항상 여기서)**: `https://script.google.com/macros/s/AKfycbxBID2kGhX5Tna7VjLbdLJIJ7qAcok3-9XifjflctLZfj5EWfxVR5NF_b5LxnVl950Atg/exec`
  - 시트 ID: `1IY1woiMdLKPQOUYMNzWoMbVBAFnECMfDiQiOP9iH4Rk` (운영 시트의 사본)
  - Apps Script 코드는 운영과 동일(`apps-script/Code.gs`), `SHEET_ID`/`ADMIN_PW`만 다름

**규칙**:
1. 브라우저에서 뭔가 저장/삭제/겹치기예약 테스트를 할 땐, 그 브라우저 탭에서 `localStorage.setItem('apiUrl', '<위 테스트 서버 URL>')`로 먼저 바꾸고 시작할 것.
2. 운영 서버 쪽은 **읽기 전용(`ping`, `loadAll`) 확인만** — 쓰기(`create`/`update`/`delete`/`saveAll`/`checkAdmin`)는 사용자가 "실서버에 반영해줘"라고 명시적으로 말했을 때만.
3. 새 세션에서 이 프로젝트를 다시 열면 **이 규칙을 가장 먼저 확인**하고 지킬 것.

## 코드 수정 방식 — 먼저 보고, 승인 후 실행
UI/기능이 바뀌는 작업은 코드를 바로 고치지 말고, 먼저 "지금 상태 / 바꾸려는 내용 / 트레이드오프"를 텍스트로 설명하고 사용자가 "그래"/"실행해" 등으로 승인한 뒤에만 파일을 수정할 것. 사소한 오타나 이미 합의된 내용의 연장은 예외.

## 프로젝트 개요
학교 특별실(강당, 운동장, 농구장, 시청각실, 소통광장, 놀이마당, 도서관, 컴퓨터실 등) 예약을 관리하는 웹앱. 선생님들이 브라우저로 접속해서 주간/월별 시간표 형태로 예약. 서버 비용 없이 무료 운영. **타깃 사용자는 4~50대 비전문가 교사** — UI는 해석이 필요한 기호(색상+범례 등)보다 그냥 읽으면 이해되는 텍스트 위주로 설계할 것.

**구조**: GitHub Pages + Vercel(이중 배포, 정적 호스팅) + Google Apps Script(서버 역할) + Google Sheets(DB)

## 파일 위치
```
/Users/kimhyunjoonn/Documents/claude/specialroom/
├── index.html      화면 구조
├── style.css       디자인 (Slate Blue 테마)
├── app.js          전체 클라이언트 로직 (제일 큼, ~1450줄)
├── api.js          Apps Script 통신
├── config.js       구글시트 연결 URL·자동연결 설정 (git 추적됨, 운영 URL 하드코딩)
├── .gitignore      apps-script/ 제외
├── README.md       설치·관리자·인수인계 안내
├── CONTEXT.md      이 파일
└── apps-script/
    └── Code.gs     서버 코드 (Apps Script에 붙여넣기용, git 제외 — 시트ID·비번 노출 방지)
```
로컬 프리뷰: `.claude/launch.json`에 `specialroom` 설정 있음 (`python3 -m http.server 8123 --directory specialroom`).

**캐시 버스팅**: `index.html`에서 `style.css?v=N`, `app.js?v=N` 형태로 버전 쿼리를 씀. **CSS/JS를 고칠 때마다 버전 숫자를 +1 해야** 브라우저가 새 파일을 받아감 (안 올리면 캐시된 옛날 파일 계속 로드됨 — 여러 번 겪은 문제). 현재: `style.css?v=39`, `app.js?v=57`, `api.js?v=22`, `config.js?v=2`.

## 서버 정보
- **운영 Google Sheets ID**: `12XFU15WU8BylhAIF2FISd-mVpAQgr8Xvz1MU0iWIFHg` (미확인 표기가 있었으나 실사용 중인 것으로 확인됨)
- **운영 Apps Script URL**: 위 "🚨 규칙" 섹션 참고
- **테스트 Apps Script URL**: 위 "🚨 규칙" 섹션 참고
- **관리자 비밀번호**: 클라이언트 코드엔 없음. `apps-script/Code.gs`의 `ADMIN_PW`에만 있고, 서버가 `checkAdmin` 액션으로 검증. 실제 값은 세션 기록에 없음(운영/테스트 서로 다름, 필요시 사용자에게 문의).

## GitHub / 배포
- **계정**: shinkim0712 / **저장소**: https://github.com/shinkim0712/specialroom
- **배포**: GitHub Pages `https://shinkim0712.github.io/specialroom/` + Vercel `https://sr-specialroom.vercel.app` (둘 다 GitHub 연동, push하면 자동 재배포)

```bash
cd /Users/kimhyunjoonn/Documents/claude/specialroom
git add . && git commit -m "수정 내용" && git push
```

Code.gs를 바꿨다면 git push와 별개로 **Apps Script 편집기에 직접 붙여넣기 + "배포 관리 → 편집 → 새 버전"** 재배포 필요. (SHEET_ID/ADMIN_PW 두 줄은 유지하고 나머지만 교체)

## 기능 목록 (2026-08-27 기준)

### 시간표 (주간 뷰)
- 월~금 × 1~8교시 (4교시는 저/고학년용 2칸 분리 → 총 9행)
- 날짜 이동: flatpickr 캘린더 팝업 클릭으로 임의 날짜 바로 이동 (기존 ±8주 드롭다운 폐기)
- 법정공휴일 자동 표시 (`KR_HOLIDAYS` 배열, **2026~2028년치 등록됨**, 이후 연도는 추가 필요)
- 학교자체 휴일 (관리자가 기간범위로 등록/삭제/**수정**)
- 예약 칸 표시: **학급(굵게) → 목적 → 이름** 순서로 표시. 색상: **학급예약=연두색, 회의/행사=파란색**

### 월별 보기 (신규)
- 상단 "월별 보기" 토글로 주간표 ↔ 월별 전환
- 달력 형태(월~금 5열, 주말 없음), 각 날짜에 그날 예약을 **"교시 학급(또는 회의/행사)"** 텍스트로 최대 5건 나열, 넘으면 "+N건 더"
- **정규수업/방과후(dateRules)는 월별 화면에 안 보여줌** — 예약 가능 여부 판단에 필요한 정보만 남김 (점유 막대+범례 방식은 "이해하기 어렵다"는 피드백으로 폐기하고 텍스트 나열로 재설계함)
- 색상 규칙 주간표와 동일 (연두/파랑)
- "이번 달" 버튼으로 오늘이 있는 달 복귀, 날짜 클릭 시 그 주 주간표로 이동

### 예약
- 이름·학급(학년/반, **반은 12반까지**)·목적 입력
- "회의/행사" 체크박스 — 체크 시 학년/반 선택창 자동 숨김, `classroom: '회의/행사'`로 저장 (구버전 데이터는 `'회의'`/`'회의/학급'`도 인식)
- 이름·학급은 브라우저에 기억되어 다음 예약에 자동 채워짐
- 예약 삭제·수정에 비밀번호 없음(의도적 정책). "수정"은 입력 폼 재사용
- 다중 선택: 빈 칸 일괄예약 + 예약된 칸 일괄삭제 (관리자·사용자 동일)
- 겹치기 예약 방지: 서버 LockService + period 타입을 `String()`으로 비교(과거 숫자 교시에서 뚫리던 버그 수정됨)

### 기간 정규시간 (dateRules) — 유일한 정규시간 편성 수단
- ~~요일별 고정 정규시간(schedule)~~ **2026-08-28 제거됨** (운영 0건이었음). 이제 정규시간은 전부 dateRules로.
- 날짜범위 + 요일 필터 + 교시 + 라벨. 수정 버튼 있음
- **모달 목록은 선택한 특별실 것만** 표시 (`#drRoom` 바꾸면 목록도 바뀜)
- **사용자도 사용 가능** (admin-only 제거). 사용자도 남의 규칙 수정/삭제 가능 (소유자 개념 없음 — 정책상 열어둠)
- **추가/수정/삭제 시 즉시 서버에 낱개 저장** (`createDateRule`/`updateDateRule`/`deleteDateRule`, autoSave 게이트). 실패 시 로컬 롤백 + 알림
- **"이 기간엔 예약 금지" 체크박스**:
  - 겹치는 예약이 있으면 **자동취소 안 하고** 목록 표시 후 저장 중단 (구 자동취소 폐지 — 8/27 사고 원인). 관리자·사용자 공통
  - 이미 예약금지된 규칙과 겹치면 "중복 추가?" 확인창
  - 정규시간 라벨이 있는 칸에 예약금지를 걸면 "그 라벨이 가려진다" 안내창 + `logs`에 기록
  - 차단은 클라이언트 레벨(서버 API 직접 호출은 안 막음)
- **겹친 규칙 표시**: 하나라도 예약금지면 무조건 예약금지 우선. 아니면 마지막에 만든 라벨 표시. 라벨 위에 라벨 얹으면 "「기존라벨」 가려짐" 안내창 + `logs` 기록

### 특별실
- **추가: 사용자도 가능** (2026-08-28~). 추가 즉시 서버에 낱개 저장(`addRoom`, 서버가 잠금 걸고 중복 확인). 실패하면 로컬 롤백
- **삭제·순서변경: 관리자 전용 유지.** 둘 다 즉시 서버에 낱개 저장(`deleteRoom`/`reorderRooms`, 서버가 현재 시트 읽고 반영 → 동시 편집해도 유실 없음)
- **순서 변경**: 탭을 0.35초 이상 꾹 눌렀다가 좌우로 끌기(Pointer Events). 실제로 순서가 바뀐 경우에만 서버 전송
- "정규시간표 서버저장" 버튼은 이제 특별실을 안 건드림

### 관리자 모드
- 비밀번호는 서버에서만 검증(`checkAdmin`)
- **관리자 모드 중엔 자동 "불러오기"가 정지됨** (편집 중 화면이 바뀌지 않도록). 사용자 모드로 돌아오면 재개. ⚠️ 정지되는 건 들어오는 쪽(load)뿐 — 내가 바꾼 예약·특별실·기간규칙은 관리자 모드에서도 즉시 서버로 나감
- **관리자 모드에서도 빈 칸 클릭 = 예약** (2026-08-28~, 요일별 정규시간 제거하면서 바뀜). 다중선택도 예약
- **변경 기록** 버튼: `logs` 시트의 삭제·수정·가림 기록을 최근순으로 보여줌

### 서버 동기화
- **실시간 낱개 저장 (autoSave 게이트, 관리자 모드에서도 나감 — 나가는 쪽은 안 막힘)**:
  - 예약: `create`/`update`/`delete`
  - 특별실: `addRoom`/`deleteRoom`/`reorderRooms`
  - 기간 정규시간: `createDateRule`/`updateDateRule`/`deleteDateRule`
- **자동 불러오기(들어오는 쪽)**: **사용자 모드에서만** 1분마다 전체 갱신(`includeSettings:true`). 관리자 모드에서는 정지(편집 중 화면 안 바뀌게)
- **페이지 최초 로드 시**: 관리자·사용자 1회 전체 최신화
- **"저장" 버튼(구 "정규시간표 서버저장", 관리자 전용)**: 이제 **학교휴일(holidays)만** 저장 (요일별 정규시간 제거됨). 안전장치 2개 — (a) 세션 중 서버 불러오기 1회 성공 전엔 **버튼 비활성**(`state.serverEverLoaded`), (b) 누르면 "학교휴일 N건 저장. 계속?" 확인창
- **변경 로그**: 서버의 삭제·수정 함수가 `logs` 시트에 한 줄씩 append (appendLog, 3000줄 캡). 라벨 가림은 클라이언트가 `log` 액션으로 기록. 누가 했는지는 안 남김
- "서버에서 불러오기": 서버→로컬 강제 덮어쓰기(확인창 뜸). **2026-08-28부터 관리자 전용**(admin-only)

### 디자인
- Slate Blue(#0e3b69) 포인트 컬러, 미니멀 카드, Hanken Grotesk(제목) + Noto Sans KR(본문)
- 휴일 색은 회색 유지(의도적)
- 예약금지 칸: 은은한 핑크 배경 + 🔒 아이콘 (대각선 빗금 무늬는 디자인상 폐기함)

## 데이터 구조 (Google Sheets)

### reservations
| id | room | date | period | name | classroom | purpose | passwordHash | createdAt |
- `passwordHash`는 죽은 필드(스키마만 남음)

### rooms
| name | order |

### schedule (죽은 시트 — 요일별 고정 정규시간, 2026-08-28 기능 제거. 시트/컬럼은 남아있으나 앱이 안 씀)
| room | dayOfWeek | period | label |

### logs (변경 기록 — 2026-08-28 추가)
| ts | action | summary |
- `action`: 예약삭제 / 예약수정 / 기간규칙삭제 / 기간규칙수정 / 특별실삭제 / 라벨가림
- 3000줄 캡, 넘으면 오래된 것부터 삭제. `?action=logs`는 최근 500줄을 최신순으로 반환

### dateRules (기간 정규시간)
| id | room | startDate | endDate | periods | daysOfWeek | label | blocked |
- `periods`, `daysOfWeek`는 JSON 문자열, `blocked`는 문자열 `'true'`/`'false'`로 저장(Code.gs `normalizeCell`에서 boolean으로 복원)

### holidays (학교자체 휴일)
| id | startDate | endDate | label |

## 교시 키값 (app.js PERIODS)
| key | label | time |
|-----|-------|------|
| 1 | 1교시 | 09:00~09:40 |
| 2 | 2교시 | 09:50~10:30 |
| 3 | 3교시 | 10:40~11:20 |
| 4A | 4교시(1) | 11:30~12:10 |
| 4E | 4교시(2) | 12:20~13:00 |
| 5EH | 5교시 | 13:10~13:50 |
| 6 | 6교시 | 14:00~14:40 |
| 7 | 7교시 | 14:50~15:30 |
| 8 | 8교시 | 15:40~16:20 |

## 알려진 버그

### ✅ 예약 중복 유령 데이터 (2026-08-28 수정, 배포 대기)
`loadFromServer` 자동 병합이 예약 고유번호(id)로만 비교해 "서버에 없는 로컬 전용 예약"을 보존 → 같은 자리에 서버+로컬 예약 중복(주간표 1개·월별표 2개).
**수정 완료**: 자동 병합 시 (room|date|period) 조합도 확인 → 서버에 같은 자리 예약이 있으면 로컬 전용 건은 버림. `loadFromServer` 내 3줄.

### ✅ "저장" 무조건 덮어쓰기 (2026-08-28 안전장치 추가, 배포 대기)
버튼 대상이 schedule·holidays 2종으로 축소 + 로딩 전 비활성 + 건수 확인창. rooms·dateRules는 실시간 낱개 저장으로 이동해 이 경로에서 빠짐.

### ✅ 예약금지 자동취소 (2026-08-28 폐지, 배포 대기)
자동취소 삭제를 없앰 → 겹치는 예약이 있으면 목록 보여주고 저장 중단. 관리자·사용자 공통.

## 사고 이력 (2026-08-27)
같은 날 두 가지 데이터 손실 사고 발생, 둘 다 사용자가 직접 발견하고 구글시트 버전 기록으로 복구함:
1. **예약 1건 삭제**: 예약금지 기능 테스트 중 만든 테스트 규칙(강당/화요일/2교시/8월)이 실제 예약(김현준, 2학년 5반, 실험)과 겹쳐서 자동취소로 삭제됨
2. **학교휴일 2건 삭제**: "정규시간표 서버저장" 테스트 중 로컬에 휴일 데이터가 덜 불러와진 상태로 저장 버튼을 눌러서 서버 값이 빈 값으로 덮어써짐

→ 재발 방지로 테스트 전용 구글시트/Apps Script 분리함 (맨 위 "🚨 규칙" 참고).

## 알려진 제약 / 의도적으로 안 고친 것
- 관리자 모드에서 직접 예약 생성 불가 (사용자 모드로 나가야 함)
- 학교자체 휴일 기간 겹침 검증 없음
- 학교휴일(holidays)은 여전히 "저장" 버튼 눌러야 서버 반영 (실시간 아님). 이제 "저장" 버튼이 담당하는 유일한 항목
- 관리자 모드에서 자동 불러오기 정지 → 오래 켜두면 화면이 낡음. 특별실 조작 시 서버 현재 목록을 안 다시 받아옴(서버 쪽 read-modify-write로 유실은 방지되지만 화면 표시는 낡을 수 있음)
- 예약금지 차단은 클라이언트 레벨만 (서버 API 직접 호출은 안 막음)
- **사용자도 남이 만든 기간 규칙을 수정/삭제 가능** (규칙에 소유자 개념 없음). 2026-08-28 논의 결과 "실수하면 관리자한테 연락" 정책으로 그대로 두기로 함 — 굳이 좁히지 않음
- ✅ (2026-08-28 수정, 배포 대기) 특별실 삭제 시 그 방의 예약·정규시간·기간규칙도 함께 삭제 (`deleteRoom`이 서버에서 4개 시트 정리, 클라이언트도 동일)

## 남은 작업 / TODO
- [ ] **2026-08-28 작업 Code.gs 배포** (맨 위 "배포 대기" 참고) — `deleteRoom` cascade purge 포함
- [ ] 설정창 `CODE_GS` 문자열 사본을 실제 `apps-script/Code.gs`와 동기화 (이미 어긋나 있고 이번에 액션 6개+cascade만큼 더 벌어짐) — 또는 "Code.gs 보기/복사" 기능 자체를 제거하고 저장소 파일로 안내
- [ ] 2029년 되면 `KR_HOLIDAYS`에 새 연도 공휴일 추가
- [ ] (선택) 학교자체 휴일 겹침 시 경고 추가할지 결정
