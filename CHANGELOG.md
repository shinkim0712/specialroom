# 변경 내역 (CHANGELOG)

특별실 예약 관리 웹앱의 업데이트 기록입니다. 최신순.

---

## 2026-08-28 (2차) — 데이터 손실 위험 정리

전체 기능을 점검해 "예약이 갑자기 사라질 수 있는" 경로를 찾아 대응했습니다.

- **"저장" 버튼 완전 제거**: 마지막까지 수동 저장이던 **학교휴일도 실시간 저장**으로 전환. 이제 모든 변경(예약·특별실·기간 정규시간·학교휴일)이 즉시 서버에 저장되며, "저장 버튼 눌러야 반영"되는 데이터가 하나도 없음
- **서버의 "통째 덮어쓰기" 기능(saveAll) 완전 삭제** — 로딩이 덜 된 화면 값으로 서버를 비우는 사고 경로 제거
- **특별실 삭제를 안전하게**: 특별실을 지울 때 딸린 예약·규칙을 "시트 전체를 비우고 다시 쓰기"가 아니라 "해당 행만 하나씩 삭제"하도록 변경. 중간에 오류가 나도 다른 예약은 안전
- **다중 선택 일괄 삭제 확인창에 목록 표시**: "3건 삭제?" → 삭제될 예약을 방·날짜·교시·이름까지 최대 15건 나열 ("되돌릴 수 없습니다" 문구 추가)
- 학교휴일 삭제·수정도 변경 기록(`logs`)에 남김

> 남은 위험 1건 (보류): 관리자 모드는 자동 새로고침이 꺼져 있어, 특별실 삭제 확인창의 "예약 N건 함께 삭제" 숫자가 낡았을 수 있음. 관리자가 최근 예약을 못 본 채 방을 삭제할 가능성.

---

## 2026-08-28 — 실시간 동기화 · 정규시간 체계 정리 · 변경 기록

이번 배치의 핵심은 **"관리자가 저장 버튼을 눌러야만 반영되던 것들을 예약처럼 실시간으로"** 바꾼 것입니다. 8/27 데이터 손실 사고(예약 자동삭제, 학교휴일 덮어쓰기)의 원인도 함께 제거했습니다.

### 실시간 서버 저장
- **기간 정규시간**: 추가·수정·삭제하는 즉시 서버에 저장 (기존: "정규시간표 서버저장" 버튼 필요)
  - **일반 사용자도 편성 가능** (기존: 관리자 전용)
  - 서버가 규칙 한 건씩 처리 — 다른 사람이 동시에 편집해도 안 부딪힘
- **특별실 추가**: 즉시 저장. **일반 사용자도 가능**
- **특별실 삭제·순서 변경**: 즉시 저장 (관리자 전용 유지). 저장 버튼 안 눌러도 됨
  - 특별실을 삭제하면 그 특별실의 **예약·기간 규칙도 함께 삭제**됨 (고아 데이터 방지). 삭제 확인창에 몇 건이 함께 지워지는지 표시

### 예약 금지 동작 개선
- **자동 취소 폐지**: 예약금지를 걸 때 겹치는 예약이 있으면, 예전엔 그 예약들을 확인 없이 삭제했음 → 이제는 **삭제하지 않고 목록만 보여준 뒤 중단**. 예약자에게 직접 요청해 예약을 지운 뒤 다시 시도
- **겹친 규칙 표시 버그 수정**: 예약금지 칸 위에 일반 라벨을 새로 만들면 예약금지(빨강 배경 + 🔒)가 화면에서 사라지던 문제 → 겹친 규칙 중 **하나라도 예약금지면 무조건 예약금지가 우선** 표시되도록 수정
- **가림 안내창**: 정규시간 라벨이 있는 칸에 예약금지를 걸거나, 라벨 위에 다른 라벨을 얹으면 "기존 「◯◯」이 화면에서 가려집니다" 안내. (규칙 자체는 안 지워지고, 위엣것을 삭제하면 아래 것이 다시 보임)

### 요일별 정규시간(schedule) 기능 제거
- 운영에서 한 건도 안 쓰이던 기능. 전부 "기간 정규시간"으로 관리 중이었음
- **관리자도 빈 칸을 클릭하면 예약** (기존: 요일별 정규시간 편집 모달). 관리자가 예약하려고 사용자 모드로 나갈 필요 없어짐
- "정규시간 편집" 버튼, 라벨 입력 모달, 다중선택 라벨 기능 삭제

### 변경 기록 (신규)
- 예약·기간 규칙·특별실이 **삭제·수정됐거나 라벨이 가려졌을 때** 구글시트 `logs` 탭에 자동 기록
- 관리자 상단 **"변경 기록"** 버튼 → 최근순 목록 확인
- **누가 했는지는 기록하지 않음** — "무엇이 없어졌나"만. 예:
  - `예약삭제 | 시청각실 · 2026-09-01 · 3교시 · 2학년 3반 · 실험 · 홍길동`
  - `기간규칙수정 | 컴퓨터실 · 라벨 "3학년 음악"→"학예회", 예약금지 설정`
  - `특별실삭제 | "무용실" — 함께 삭제: 예약 3 / 기간규칙 5`
- 3000줄까지 보관, 넘으면 오래된 것부터 삭제

### 그 외
- **기간 정규시간 목록**: 모달에서 선택한 특별실의 규칙만 표시 (기존: 전체 100여 건 나열)
- **"저장" 버튼**: 이름 단축(구 "정규시간표 서버저장"), 위치를 관리자 버튼 왼쪽으로. 담당 항목은 **학교휴일만** 남음
  - 안전장치: 서버에서 한 번도 못 불러온 상태에선 버튼 비활성 / 누르면 저장 건수 확인창 (빈 값으로 덮어쓰는 사고 방지)
- **"서버에서 불러오기" 버튼**: 관리자 전용으로 숨김 (일반 사용자가 실수로 눌러 화면이 초기화되는 것 방지)
- **예약 유령 데이터 버그 수정**: 같은 칸 예약이 주간표엔 1개, 월별 보기엔 2개로 보이던 현상 (서버·로컬 병합 로직을 방·날짜·교시 기준으로 비교하도록 수정)
- 안내창·확인창 문구를 정중한 존댓말로 통일
- 설정창의 "Code.gs 보기/복사" 기능 제거 (실제 서버 코드와 계속 어긋나서). 서버 코드는 저장소의 `apps-script/Code.gs` 사용

---

## 2026-08-27 — 월별 보기 재설계

- 월별 보기를 **색상 막대 + 범례** 방식에서 **텍스트 목록**("교시 학급" 그대로 나열)으로 재설계 — "색으로 표시하니 이해하기 어렵다"는 피드백 반영
- 정규수업/방과후는 월별 화면에서 숨김 (예약 가능 여부 판단에 필요한 정보만)
- 예약 색상을 주간표와 통일 (학급예약=연두, 회의/행사=파랑)

---

## 2026-08-25 — 예약 칸 표시 정리

- 주간표 예약 칸 표시 순서: **학급(굵게) → 목적 → 이름**
- 예약 체크박스 문구: "회의" → "회의/행사"
- 기간 정규시간·학교휴일에 **수정** 기능 추가
- 반 선택을 **12반까지** 확대
- 월별 보기에 "이번 달" 버튼 추가

---

## 2026-08-23 — 대규모 업데이트

- **서버 관리자 인증**: 관리자 비밀번호를 브라우저 코드에서 제거하고 서버(Apps Script)에서만 검증
- **겹치기 예약 방지**: 서버 LockService로 동시 예약 차단
- **기간 정규시간**: 날짜 범위 + 요일 + 교시로 정규시간 라벨 설정, "이 기간엔 예약 금지" 옵션
- **학교자체 휴일 관리**: 방학·재량휴업일 등을 기간으로 등록 (법정공휴일은 자동 표시)
- **월별 보기** 추가
- 날짜 이동을 ±8주 드롭다운에서 **캘린더 팝업**으로 교체
- 관리자 **특별실 순서 이동** (탭 롱프레스 후 드래그)
- 2027·2028년 법정공휴일·대체공휴일 등록
- 관리자/사용자 모드별 자동 동기화 정책 정리
- 각종 버그 수정 (주차 선택 고정 버그, 캘린더 팝업 모바일 잘림, 드래그 재정렬 등)

---

## 2026-05-22 ~ 05-24 — 초기 개발

- 특별실 예약 관리 웹앱 최초 배포 (GitHub Pages + Vercel + Google Apps Script + Google Sheets)
- 주간 시간표, 예약 생성·수정·삭제, 내 예약 보기
- 다중 선택 일괄 예약
- 4교시 저/고학년 분리 등 교시 구조
- 자동 저장·자동 불러오기, 구글시트 연동 설정 및 도움말
